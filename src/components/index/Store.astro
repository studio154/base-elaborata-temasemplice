<section id="HeaderThree" aria-labelledby="HeaderThree">
  <div
    class="relative px-8 py-12 mx-auto sm:px-6 md:max-w-2xl md:px-4 lg:max-w-6xl lg:px-20"
  >
    <div class="grid grid-cols-1 gap-y-10 gap-x-6 pt-10 lg:grid-cols-3">
      <h2 class="text-xl text-black">Screenshots</h2>
      <div class="grid grid-cols-1 gap-8 sm:grid-cols-2 lg:col-span-2">
        <img
          src="/assets/screenshots/one.png"
          alt="This is a detail image"
          class="aspect-[384/246] rounded-lg bg-slate-200 ring-1 ring-slate-900/10"
        /><img
          src="/assets/screenshots/two.png"
          alt="This is a detail image"
          class="aspect-[384/246] rounded-lg bg-slate-200 ring-1 ring-slate-900/10"
        /><img
          src="/assets/screenshots/three.png"
          alt="This is a detail image"
          class="aspect-[384/246] rounded-lg bg-slate-200 ring-1 ring-slate-900/10"
        /><img
          src="/assets/screenshots/four.png"
          alt="This is a detail image"
          class="aspect-[384/246] rounded-lg bg-slate-200 ring-1 ring-slate-900/10"
        />
      </div>
    </div>
  </div>
</section>

<section>
  <div
    class="relative px-8 py-12 mx-auto sm:px-6 md:max-w-2xl md:px-4 lg:max-w-6xl lg:px-20 lg:pt-32"
  >
    <section
      class="grid grid-cols-1 items-baseline gap-y-10 gap-x-6 lg:grid-cols-3"
    >
      <div class="lg:pr-24 md:pr-12">
        <h2 class="font-semibold text-zinc-600 font-display text-3xl">
          Frequently asked questions
        </h2>
      </div>
      <div class="col-span-2">
        <div x-data="{ active: false }" class="mx-auto w-full space-y-4">
          <div class="space-y-3">
            <div class="font-semibold text-zinc-400 text-sm">General</div>

            <div
              x-data="{
          id: 1,
          get expanded() {
              return this.active === this.id
          },
          set expanded(value) {
              this.active = value ? this.id : null
          },
      }"
              role="region"
            >
              <h2>
                <button
                  x-on:click="expanded = !expanded"
                  :aria-expanded="expanded"
                  class="flex w-full items-center justify-between font-semibold text-xl text-black focus:text-blue-500"
                >
                  <span>Can I buy only this template?</span>
                  <span x-show="expanded" aria-hidden="true" class="ml-4"
                    >&minus;
                  </span>
                  <span x-show="!expanded" aria-hidden="true" class="ml-4"
                    >&plus;
                  </span>
                </button>
              </h2>

              <div x-show="expanded" x-collapse>
                <div class="text-sm text-slate-500 pt-5">
                  Absolutely, you can purchase the template from the navigation
                  button or <a href="https://supreme.lemonsqueezy.com/checkout/buy/59105270-55f2-42b0-9728-3094f40f86f4" class="text-blue-500 hover:text-black"
                    >click here.
                  </a>
                </div>
              </div>
            </div>

            <div
              x-data="{
        id: 2,
        get expanded() {
            return this.active === this.id
        },
        set expanded(value) {
            this.active = value ? this.id : null
        },
    }"
              role="region"
            >
              <h2>
                <button
                  x-on:click="expanded = !expanded"
                  :aria-expanded="expanded"
                  class="flex w-full items-center justify-between font-semibold text-xl text-black focus:text-blue-500"
                >
                  <span>Is there a template demo available?</span>
                  <span x-show="expanded" aria-hidden="true" class="ml-4"
                    >&minus;
                  </span>
                  <span x-show="!expanded" aria-hidden="true" class="ml-4"
                    >&plus;
                  </span>
                </button>
              </h2>

              <div x-show="expanded" x-collapse>
                <div class="text-sm text-slate-500 pt-5">
                  You are previewing the whole template right now, <a
                    href="/overview"
                    class="text-blue-500 hover:text-black"
                    >click here.
                  </a> to se all the demo pages and sections on the live version,
                </div>
              </div>
            </div>
            <div
              x-data="{
        id: 3,
        get expanded() {
            return this.active === this.id
        },
        set expanded(value) {
            this.active = value ? this.id : null
        },
    }"
              role="region"
            >
              <h2>
                <button
                  x-on:click="expanded = !expanded"
                  :aria-expanded="expanded"
                  class="flex w-full items-center justify-between font-semibold text-xl text-black focus:text-blue-500"
                >
                  <span>What does "free updates" mean?</span>
                  <span x-show="expanded" aria-hidden="true" class="ml-4"
                    >&minus;
                  </span>
                  <span x-show="!expanded" aria-hidden="true" class="ml-4"
                    >&plus;
                  </span>
                </button>
              </h2>

              <div x-show="expanded" x-collapse>
                <div class="text-sm text-slate-500 pt-5">
                  When you purchase any supreme product, any improvements to
                  that product will always be free for anyone who owns that
                  product.
                </div>
                <div class="text-sm text-slate-500 pt-5">
                  <strong>Individual templates:</strong> When you buy an individual
                  template, any improvements we make to that template will be available
                  to you as part of your original purchase.
                </div>
                <div class="text-sm text-slate-500 pt-5">
                  So if a bug gets fixed or upgrade the template to use the
                  latest version of Astro, Alpinejs or Tailwind CSS you can
                  download an updated version of the template free of charge.
                </div>
                <div class="text-sm text-slate-500 pt-5">
                  Any new templates released are considered separate products,
                  and can be purchased separately or you can also buy the <a
                    href="#"
                    class="text-blue-500 hover:text-black"
                    >Bundle.
                  </a> whic includes all templates.
                </div>
                <div class="text-sm text-slate-500 pt-5">
                  <strong>Supreme bundle:</strong> When you purchase the Supreme
                  Bunlde license, you get access to every site template available
                  today, plus any new templates or component packages released in
                  the future at no additional cost.
                </div>
                <div class="text-sm text-slate-500 pt-5">
                  This means that if a brand new social media website template
                  or new component package is added to Supreme Bundle ( all
                  templates are inclued in the Bundle ), access to those
                  products are included in your original purchase, with no
                  upgrade cost.
                </div>
              </div>
            </div>

            <div class="font-semibold text-blue-400 pt-12 text-sm">
              Compatibility
            </div>
            <div
              x-data="{
        id: 4,
        get expanded() {
            return this.active === this.id
        },
        set expanded(value) {
            this.active = value ? this.id : null
        },
    }"
              role="region"
            >
              <h2>
                <button
                  x-on:click="expanded = !expanded"
                  :aria-expanded="expanded"
                  class="flex w-full items-center justify-between font-semibold text-xl text-black focus:text-blue-500"
                >
                  <span>Are design files included?</span>
                  <span x-show="expanded" aria-hidden="true" class="ml-4"
                    >&minus;
                  </span>
                  <span x-show="!expanded" aria-hidden="true" class="ml-4"
                    >&plus;
                  </span>
                </button>
              </h2>

              <div x-show="expanded" x-collapse>
                <div class="text-sm text-slate-500 pt-5">
                  No, design assets for tools like Figma, Sketch, Penpot or
                  Adobe XD are not included.
                </div>

                <div class="text-sm text-slate-500 pt-5">
                  I do not offer or produce design files as part of my own
                  design and development process, so building these extra
                  resources means I can't spend as much time creating new
                  templates in code which is where I believes I can provide the
                  most value.
                </div>
              </div>
            </div>
            <div
              x-data="{
        id: 5,
        get expanded() {
            return this.active === this.id
        },
        set expanded(value) {
            this.active = value ? this.id : null
        },
    }"
              role="region"
            >
              <h2>
                <button
                  x-on:click="expanded = !expanded"
                  :aria-expanded="expanded"
                  class="flex w-full items-center justify-between font-semibold text-xl text-black focus:text-blue-500"
                >
                  <span>What JS framework is used?</span>
                  <span x-show="expanded" aria-hidden="true" class="ml-4"
                    >&minus;
                  </span>
                  <span x-show="!expanded" aria-hidden="true" class="ml-4"
                    >&plus;
                  </span>
                </button>
              </h2>

              <div x-show="expanded" x-collapse>
                <div class="text-sm text-slate-500 pt-5">
                  I am not using any Framework, all the templates are built
                  using Astro.Build but some of the components are built using
                  Alpine.js. I am using Alpinejs on toggles, tabs, sliders,
                  pricing table toggles, hamburguer menus,...
                </div>

                <div class="text-sm text-slate-500 pt-5">
                  I belive that to make the templates less opinionated, I would
                  leave the customer to choose the stack they want now that you
                  can add any framework on Astro anytime.
                </div>
              </div>
            </div>
            <div
              x-data="{
        id: 6,
        get expanded() {
            return this.active === this.id
        },
        set expanded(value) {
            this.active = value ? this.id : null
        },
    }"
              role="region"
            >
              <h2>
                <button
                  x-on:click="expanded = !expanded"
                  :aria-expanded="expanded"
                  class="flex w-full items-center justify-between font-semibold text-xl text-black focus:text-blue-500"
                >
                  <span>What version of the techs are you using?</span>
                  <span x-show="expanded" aria-hidden="true" class="ml-4"
                    >&minus;
                  </span>
                  <span x-show="!expanded" aria-hidden="true" class="ml-4"
                    >&plus;
                  </span>
                </button>
              </h2>

              <div x-show="expanded" x-collapse>
                <div class="text-sm text-slate-500 pt-5">
                  The templates are always going to be updated to the last
                  versions available if this ones have breaking changes.
                </div>
              </div>
            </div>
            <div
              x-data="{
        id: 7,
        get expanded() {
            return this.active === this.id
        },
        set expanded(value) {
            this.active = value ? this.id : null
        },
    }"
              role="region"
            >
              <h2>
                <button
                  x-on:click="expanded = !expanded"
                  :aria-expanded="expanded"
                  class="flex w-full items-center justify-between font-semibold text-xl text-black focus:text-blue-500"
                >
                  <span>What browsers are supported?</span>
                  <span x-show="expanded" aria-hidden="true" class="ml-4"
                    >&minus;
                  </span>
                  <span x-show="!expanded" aria-hidden="true" class="ml-4"
                    >&plus;
                  </span>
                </button>
              </h2>

              <div x-show="expanded" x-collapse>
                <div class="text-sm text-slate-500 pt-5">
                  The templates in Supreme are designed to work in the latest,
                  stable releases of all major browsers, including Chrome,
                  Firefox, Safari, Arc and Edge.
                </div>
              </div>
            </div>

            <div class="font-semibold text-blue-400 pt-12 text-sm">
              Licensing
            </div>
            <div
              x-data="{
        id: 8,
        get expanded() {
            return this.active === this.id
        },
        set expanded(value) {
            this.active = value ? this.id : null
        },
    }"
              role="region"
            >
              <h2>
                <button
                  x-on:click="expanded = !expanded"
                  :aria-expanded="expanded"
                  class="flex w-full items-center justify-between font-semibold text-xl text-black focus:text-blue-500"
                >
                  <span>How do your licenses work?</span>
                  <span x-show="expanded" aria-hidden="true" class="ml-4"
                    >&minus;
                  </span>
                  <span x-show="!expanded" aria-hidden="true" class="ml-4"
                    >&plus;
                  </span>
                </button>
              </h2>

              <div x-show="expanded" x-collapse>
                <div class="text-sm text-slate-500 pt-5">
                  Read about the licenses <a
                    href="https://supremeui.dev/license.html"
                    class="text-blue-500 hover:text-black"
                    >right here.
                  </a> to se all the demo pages and sections on the live version,
                </div>
              </div>
            </div>

            <div class="font-semibold text-blue-400 pt-12 text-sm">Support</div>
            <div
              x-data="{
        id: 9,
        get expanded() {
            return this.active === this.id
        },
        set expanded(value) {
            this.active = value ? this.id : null
        },
    }"
              role="region"
            >
              <h2>
                <button
                  x-on:click="expanded = !expanded"
                  :aria-expanded="expanded"
                  class="flex w-full items-center justify-between font-semibold text-xl text-black focus:text-blue-500"
                >
                  <span> Please email us at</span>
                  <span x-show="expanded" aria-hidden="true" class="ml-4"
                    >&minus;
                  </span>
                  <span x-show="!expanded" aria-hidden="true" class="ml-4"
                    >&plus;
                  </span>
                </button>
              </h2>

              <div x-show="expanded" x-collapse>
                <div class="text-sm text-slate-500 pt-5">
                  <a
                    href="mailto:michael@supremeui.dev"
                    class="text-blue-500 hover:text-black"
                    >right here.
                  </a> to se all the demo pages and sections on the live version,
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>
    </section>
  </div>
</section>
